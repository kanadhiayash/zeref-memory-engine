schema: zeref.policy/v1
id: balanced
version: 1

max_parallel_workers: 4
max_capabilities: 6
independent_verifiers: 1
autonomy_default: auto-safe

reasoning_class_limits:
  fast: unlimited
  balanced: unlimited
  deep: 5
  frontier: 0

cost_envelope:
  usd_max: 2.50
  tokens_input_max: 1000000
  tokens_output_max: 200000

description: Typical project work. One independent verifier. Deep allowed for high-risk steps.
