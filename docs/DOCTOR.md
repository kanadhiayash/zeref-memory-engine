# Doctor

`zeref doctor` reports local installation and project health.

It checks:

- Python version
- package import
- config folder
- privacy files
- memory directories and files
- state schema
- audit logs
- permissions config

Doctor checks are local-only and do not send project data anywhere.
